<div class="shadow-sm">
  <table class="table table-borderless bg-white mb-0">
    <thead class="contents-head">
      <tr>
        <th>新着記事</th>
      </tr>
    </thead>
    <tbody>
      <% items.each do |item| %>
        <tr>
          <!--管理者がサインインしている場合-->
          <% if admin_signed_in? %>
            <td><%= link_to item.name, admin_item_path(item.id) %></td>
          <% else %>
            <td><%= link_to item.name, item_path(item.id) %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>